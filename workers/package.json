{
  "name": "drclash-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply drclash-db",
    "migrate:local": "wrangler d1 migrations apply drclash-db --local"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241001.0",
    "wrangler": "^3.80.0",
    "typescript": "^5.6.0"
  }
}
